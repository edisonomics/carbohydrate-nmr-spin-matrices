# Spinach dependency

Spinach is a third-party MATLAB library and is intentionally not distributed
with this repository. Install Spinach separately, then set `SPINACH_ROOT` to
that local installation before running the MATLAB simulations.

Example:

```bash
export SPINACH_ROOT="$HOME/Spinach"
```
