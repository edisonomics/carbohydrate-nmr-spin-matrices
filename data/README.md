# Local data

Raw Bruker NMR datasets are intentionally excluded from GitHub. Download the
datasets from NAN, preserve their native Bruker directory structure, and import
them with the repository's data-import tools. Do not commit raw `fid`, `ser`,
`acqus`, `1r`, or other instrument files.

Start from the repository root and follow the workflow in the main README.
